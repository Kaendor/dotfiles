return {
  'mvllow/modes.nvim',
  config = function()
    require('modes').setup {
      colors = {
        -- bg = '',
        -- copy = latte.mauve,
        -- delete = latte.maroon,
        -- insert = latte.green,
        -- visual = latte.mauve,
      },
    }
  end,
}
